apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: atlas-evidence-smoke
  namespace: warehouse
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-data
  resources:
    requests:
      storage: 5Gi
