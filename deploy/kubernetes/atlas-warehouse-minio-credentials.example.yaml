apiVersion: v1
kind: Secret
metadata:
  name: atlas-warehouse-minio-credentials
  namespace: atlas-warehouse
type: Opaque
stringData:
  # Replace both placeholders before applying. Never commit real credentials.
  root-user: REPLACE_WITH_MINIO_ROOT_USER
  root-password: REPLACE_WITH_MINIO_ROOT_PASSWORD
